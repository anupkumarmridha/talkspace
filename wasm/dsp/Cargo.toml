[package]
name = "talkspace-dsp"
version = "1.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"     # this ships to phones over mobile data; size is latency
lto = true
codegen-units = 1
panic = "abort"
strip = true
